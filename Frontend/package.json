{
  "name": "memoq-ai-sidecar-frontend",
  "version": "1.0.0",
  "description": "CodeMirror 6 based editor with Ghost Text AI autocompletion",
  "type": "module",
  "scripts": {
    "build": "esbuild editor.js --bundle --outfile=editor.bundle.js --format=esm"
  },
  "dependencies": {
    "@codemirror/state": "^6.4.1",
    "@codemirror/view": "^6.28.0",
    "codemirror": "^6.0.1"
  },
  "devDependencies": {
    "esbuild": "^0.21.0"
  }
}
